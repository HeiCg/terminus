{
  "name": "terminus-monorepo",
  "version": "0.1.1",
  "private": true,
  "workspaces": [
    "collector",
    "cli"
  ],
  "scripts": {
    "start": "npm start -w collector",
    "test": "npm run test -w collector && npm run test -w cli",
    "typecheck": "npm run typecheck -w collector && npm run typecheck -w cli",
    "lint": "npm run lint -w collector && npm run lint:ui -w collector && npm run lint -w cli",
    "check:ui": "npm run check:ui -w collector",
    "lint:ui": "npm run lint:ui -w collector",
    "test:browser": "npm run test:browser -w collector"
  }
}
